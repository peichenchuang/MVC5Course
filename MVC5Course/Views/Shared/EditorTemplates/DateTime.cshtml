@model DateTime?

@{ 
    var htmlArrtibutes = new
    {
        @class = "form-control text-box single-line valid",
        type = "date"
    };
}

@if(Model.HasValue)
{
    @Html.TextBox("", Model.Value.ToString("yyyy-MM-dd"), htmlArrtibutes)
}
else
{
    @Html.TextBox("", null, htmlArrtibutes)
}